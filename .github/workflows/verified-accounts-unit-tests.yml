name: Verified Accounts Unit Tests

on:
  push:
    branches: [main]
    paths:
      - 'contracts/verified-accounts/src/**'
      - 'contracts/verified-accounts-interface/**'
      - '.github/workflows/verified-accounts-unit-tests.yml'
  pull_request:
    branches: [main]
    paths:
      - 'contracts/verified-accounts/src/**'
      - 'contracts/verified-accounts-interface/**'
      - '.github/workflows/verified-accounts-unit-tests.yml'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  unit-tests:
    name: Verified Accounts Unit Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.86.0
          components: clippy

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            contracts/verified-accounts/target
          key: ${{ runner.os }}-cargo-verified-accounts-unit-${{ hashFiles('contracts/verified-accounts/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-verified-accounts-unit-

      - name: Run unit tests
        working-directory: contracts/verified-accounts
        run: cargo test --lib -- --nocapture

      - name: Run clippy
        working-directory: contracts/verified-accounts
        run: cargo clippy --lib -- -D warnings
